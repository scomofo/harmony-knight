# Active Session State

- **Task**: Codebase review (branch `claude/harmony-knight-review-5ji6do`) + fixes for the review's findings
- **Status**: Review delivered; six of the review's recommendations implemented across two commits
- **Completed this session**:
  - Atomic writes in `lib/engine/persistence.dart` (write-to-temp-then-rename for `saveProgress`, `recordSession`, `recordEngagement`, `saveSRItems`) so a crash mid-write can't corrupt a JSON store
  - `lib/core/router.dart`: `SharedPreferences.getInstance()` is now only called on the `/` redirect branch instead of on every navigation
  - `lib/main.dart`: global error boundary — `FlutterError.onError`, `WidgetsBinding.instance.platformDispatcher.onError`, and a release-mode `ErrorWidget.builder` fallback
  - Extracted `lib/engine/core/practice_question_engine.dart` (`PracticeQuestionEngine`) from `_PracticeScreenState` — owns note-pool selection, SR queueing/question generation, and weak-note tracking as a framework-agnostic, unit-testable class. `PracticeScreen`'s build/dialog/animation code is unchanged; only `initState`/`_rebuildSRQueue`/`_generateQuestion`/`_handleAnswer` were rewired to delegate.
  - `test/engine/practice_question_engine_test.dart` (new): unit tests for the extracted engine — grade-band note pools, question generation, the SR-queue empty-queue fallback, and `recordAnswer`'s correctness/scheduling/weak-note behavior (including the "hard" vs "good" SR response distinction and the ≥3-attempts/>50%-miss-rate weak-note rule).
  - Accessibility: `lib/screens/practice_screen.dart` now wraps the target-note display and each answer button in `Semantics` (button role, note-name label, enabled state). The target's label respects the same `confidence < 0.5` gate as its visual name hint (so a screen reader isn't told the answer once the visual hint has faded for a high-confidence player); the now-redundant hint `Text` is wrapped in `ExcludeSemantics` to avoid a duplicate announcement. Answer buttons always expose their note name (there's no way to choose between unlabeled buttons otherwise). Two new tests in `test/screens/practice_screen_test.dart` (`PracticeScreen — accessibility` group) verify both labels via `tester.ensureSemantics()` / `find.bySemanticsLabel`.
- **Not done / deliberately out of scope**: `ScaffoldedNote` itself was deliberately left without its own built-in semantics label (to avoid leaking the target note's identity at high confidence from other call sites, and to avoid scope creep into other screens that reuse it without an audit).
- **Persistence migration — investigated via `/architecture-decision`, declined**: ran the ADR flow for "migrate persistence to Isar/Hive" (the one review recommendation not treated as a quick fix) and discovered `docs/architecture/adr-0003-json-file-persistence.md` already made this exact call — it explicitly considered and rejected Isar for MVP, with documented trigger conditions to revisit (session history > 10k records, measurable query slowness, or added cloud sync). None of those are true yet (pre-launch, sprint-10, 500/2000 caps by design, no cloud sync in scope). My original review recommendation was made without reading ADR-0003 and was premature. User agreed to stand down — no new ADR was written; ADR-0003's decision stands. Re-raise only if one of its stated trigger conditions is actually met.
- **Verification note**: no Dart/Flutter SDK is available in this sandbox (no `flutter`/`dart` on PATH, no vendored SDK) — every code change across both commits on this branch was hand-verified by careful reading (including manual brace/paren balance checks) rather than by running `flutter analyze`/`flutter test`. Whoever picks this up next should run the real test suite to confirm, especially the new semantics tests (`tester.ensureSemantics()` / `find.bySemanticsLabel` — APIs believed correct but unverified in this sandbox).
- **Next recommended step**: run `flutter analyze && flutter test` to confirm both commits. No further architecture follow-up is pending from the original review.
